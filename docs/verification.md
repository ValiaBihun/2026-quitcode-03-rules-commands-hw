# Перевірка (Task B, C і бонус E)

> Сюди — лише те, що справді сталося в сесії: цитати, числа, імена файлів.

**Інструмент:** Claude Code (desktop app, Code tab), модель Opus 5
**Базова лінія до роботи:** `npm test` → 18 passed (6 файлів);
`npm run check:rules` → `TOTAL: 8 violation(s)`; `npm run typecheck` → без помилок.

> ⚠️ **Чесне застереження про форму прогонів.** Правила й команди створювались
> усередині першої сесії, а Claude Code читає `.claude/commands/` і `.claude/rules/`
> на **старті** сесії. Тому виклик `/analyze-error` там повернув
> `Unknown skill: analyze-error` — команда ще не була зареєстрована. Прогони Task C
> нижче виконані **покроково за текстом файлів команд**, з фіксацією реальних чисел.
>
> Після перезапуску сесії всі три команди зареєструвались і видно їх у переліку
> доступних:
>
> ```
> analyze-error        — Лог або стек → корінна причина: файл, рядок, механізм...
> generate-integration — Нова інтеграція за архітектурою проєкту...
> refactor             — Привести файл до конвенцій проєкту без зміни поведінки...
> ```
>
> Тобто `description` із frontmatter підтягується коректно. Рядок «Чи підставився
> `$ARGUMENTS`» лишається до повторного прогону вже через `/`.

---

## Task B — чи бачить інструмент AGENTS.md

- Інструмент і версія: Claude Code (desktop app), модель Opus 5
- Що було до: `CLAUDE.md` містив markdown-**посилання** `[AGENTS.md](./AGENTS.md)`.
  Посилання не є імпортом — Claude Code не завантажує файл за ним, агент бачить
  лише рядок тексту. Це і був баг стартера.
- Що зроблено: у `CLAUDE.md` поставлено **імпорт окремим рядком** — `@AGENTS.md`;
  нижче лишилось тільки специфічне для Claude Code (перелік правил, команд, хук).
  `AGENTS.md` переписано з «пиши якісний код» на 58 рядків конкретики.
- Як перевірити: нова сесія → які файли інструкцій проєкту фактично завантажено
  (`/context` → **Memory files**); і контрольне питання агентові без відкриття файлів.
- **Результат (нова сесія, 20.09.2026):** завантажено рівно **три** файли
  інструкцій проєкту:

  ```
  CLAUDE.md                      (project instructions, checked into the codebase)
  AGENTS.md                      (project instructions, checked into the codebase)
  .claude/rules/do-not-touch.md  (project instructions, checked into the codebase)
  ```

  `AGENTS.md` потрапив у контекст **не сам по собі** — Claude Code читає лише
  `CLAUDE.md`, а далі йде по рядку `@AGENTS.md`. До виправлення на його місці було
  markdown-посилання, і файл не завантажувався. Тобто імпорт працює, і це видно
  на фактичному вмісті контексту, а не з опису.

- Контрольне питання агентові в тій самій сесії («які правила проєкту вже є в
  тебе в контексті? файли не відкривай») — відповів переліком із `do-not-touch.md`
  (захищені зони, процедура зупинки, заборона `--write-lock`) і семи однорядкових
  правил з `AGENTS.md`, і сам зазначив:

  > «Деталі шарів, публічного API ядра й конвенцій коду/тестів у контексті
  > відсутні — вони в `architecture.md` і `conventions.md` з
  > `paths: app/src/**/*.ts`.»

---

## Task A — режими застосування правил

Та сама перевірка з нової сесії підтверджує пункт 3 умови Task A — режими обрано
свідомо, і вони справді працюють:

| Правило | Режим | Чи в контексті на старті | Чому так і треба |
|---|---|---|---|
| `do-not-touch.md` | без frontmatter | **так** | захист має діяти в кожній сесії, зокрема в тих, що не торкаються `app/src/**` |
| `architecture.md` | `paths: app/src/**/*.ts` | ні | підтягнеться при роботі з кодом застосунку |
| `conventions.md` | `paths: app/src/**/*.ts` | ні | те саме — інакше це зайвий контекст у кожній сесії |

**Друга половина доказу — правила за `paths` таки підтягуються.** Під час прогону
`/analyze-error` (нижче) агент прочитав `app/src/sync/state.ts`, і обидва правила
з `paths: app/src/**/*.ts` автоматично зайшли в контекст — без `@`-згадки й без
відкриття їхніх файлів. Тобто режим працює в обидва боки: у сесії без коду
застосунку правил немає, у сесії з кодом вони з'являються самі.

Це і є різниця, за яку рубрика знімає бали: якби всі три були always-on, агент
тягав би конвенції коду в сесію про CI чи документацію.

---

## Task C — прогони команд

### `/analyze-error materials/error-log.txt`

- Виклик: `/analyze-error materials/error-log.txt` — **справжній прогін через `/`**
  у сесії, де команда вже зареєстрована (20.09.2026).
- **Чи підставився `$ARGUMENTS`: так.** У завантаженому тілі команди рядок «Ціль»
  прийшов уже розкритим:

  ```
  **Ціль:** materials/error-log.txt
  ```

  Тобто запасний варіант із шаблону («якщо плейсхолдер не підставився — візьми
  ціль із тексту після назви команди») у Claude Code не знадобився.
- **Побічне спостереження:** щойно агент прочитав `app/src/sync/state.ts`,
  у контекст самі зайшли `architecture.md` і `conventions.md` — ті, що з
  `paths: app/src/**/*.ts`. Це підтверджує режим прив'язки за шляхами на практиці.
- **Тригер:** `2026-09-10T00:02:37Z` — нічний бекап забив диск
  (`disk usage /srv/lead-sync/data: 97%`, о 00:05 — 100%). О 00:05 і 00:10 прогін
  падає з `ENOSPC` у `saveState` (`app/src/sync/state.ts:21`), викликаному з
  `runSync` (`app/src/sync/run.ts:18`).
- **Корінна причина:** `app/src/sync/state.ts:13–17` — `loadState()` обгортає
  `JSON.parse(readFileSync(...))` у `try/catch`, і в `catch` **мовчки повертає**
  `INITIAL_STATE` з `lastSyncedAt: "1970-01-01T00:00:00.000Z"`.
  Механізм: `writeFileSync` спершу обрізає файл, а вже потім пише, тож падіння на
  `ENOSPC` лишило `data/sync-state.json` порожнім/побитим. Після цього кожен
  наступний запуск читає порожній файл, тихо відкочує курсор у 1970 рік і вважає
  **всі ~1300 лідів** новими — звідси дублікати в Slack і в таблиці.
- **Чому це не тригер:** о `00:13:48` ops звільнили диск (`disk usage: 41%`), а
  дублікати йшли ще до `09:14` — **дев'ять годин** після зникнення тригера.
  Місце на диску не було тим, що тримало систему зламаною.
- **Чому воно не полагодилось саме (друга половина механізму):** `runSync` пише
  новий `lastSyncedAt` **лише в кінці** прогону (`app/src/sync/run.ts`, останній
  `saveState`). Розсилка ~1300 лідів × 2 інтеграції впирається в ліміт Slack
  (`HTTP 429` о 00:18) і планувальник вбиває процес на 4м30с
  (`run exceeded 4m30s limit, worker process killed (SIGKILL)`) — **до** того
  запису. Стан не лагодиться ніколи, і цикл самопідтримується кожні 5 хвилин.
- **Яку конвенцію порушено:** «Зовнішнім даним не довіряємо» — будь-який JSON лише
  через `parseJson(text, guard)`, а невалідний JSON це `Result{ok:false}`, який
  видно в журналі; тихий дефолт заборонений. Цей самий рядок ловить і
  `check:rules`: `src/sync/state.ts  json-via-parse  line 14`. Це **останнє**
  порушення, що лишилось у проєкті.
- **Тест, що відтворює:** `app/src/sync/state.test.ts` — записати у тимчасовий файл
  порожній рядок (як після обірваного `writeFileSync`) і викликати `loadState(path)`.
  Зараз повертає `{ lastSyncedAt: "1970-01-01T00:00:00.000Z" }` мовчки; очікування —
  помилка назовні, а не відкат курсора. Рівнем вище: `runSync` з побитим станом і
  трьома старими лідами зараз звітує `pending: 3` замість `pending: 0`.
- **План виправлення (не застосований):** `loadState` повертає `Result<SyncState>`
  через `parseJson` з guard на `{ lastSyncedAt: string }`; `runSync` на
  `ok: false` **зупиняє прогін** з помилкою в журналі замість розсилки з нуля;
  окремо — зберігати курсор інкрементально, щоб убитий прогін не втрачав прогрес.
  Обидва файли в `app/src/sync/`, ядро чіпати не треба.
- **Чи зупинився там, де сказано в Stop:** так. Після звіту агент зупинився й
  попросив підтвердження. `git status --short` під час прогону — порожній, файл
  `state.test.ts` не створювався. Порушення `json-via-parse` у `state.ts:14`
  лишилось у `check:rules` навмисно — як доказ, що звіт не перетворився на
  самовільний фікс.
- **Acceptance criteria команди — усі п'ять виконані:** названо файл і рядок
  (`state.ts:13-17`, механізм через `state.ts:21` і `run.ts:20/36`); доказ із
  хронології (тригер зник о 00:13:48, симптом тривав до 09:14:30); названо
  конвенцію; описано тест і чому він падає; робоча тека чиста.
- **Чи знадобилась ітерація над командою:** ні. Крок 3 «відріж тригер від причини»
  з трьома контрольними питаннями (чи тривав симптом після зникнення тригера;
  який рядок вирішив працювати з побитим станом; що мало б статися за правилами)
  вивів на причину з першого прогону. Без цього кроку природна відповідь —
  «закінчилось місце на диску», тобто тригер.

### `/refactor` — два прогони

#### Прогін 3.1 — `/refactor app/src/integrations/sheets-append.ts` (за текстом команди)

- Виклик: `/refactor app/src/integrations/sheets-append.ts`
- `npm run check:rules` для цього файлу: **7 → 0**
  (до: `no-any` ×2, `http-via-core`, `env-via-config`, `json-via-parse`,
  `log-via-logger` ×2). `TOTAL` по проєкту: **8 → 1**.
- `npm test` до / після: **18 passed / 18 passed**, 6 файлів, кількість та сама.
- `npm run typecheck`: без помилок і до, і після.
- Що замінено:
  - `fetch(...)` → `postJson(url, body)` з `core/http.ts` (таймаут, повтори на 5xx/429);
  - `process.env.SHEETS_WEBHOOK_URL` / `SHEETS_TOKEN` → два виклики `readEnv()`;
  - `JSON.parse(await res.text())` → `parseJson(response.value, isSheetsResponse)`
    з guard на `{ status: string }`, складеним з `isRecord` + `isString`;
  - `console.log` ×2 → `log.info` / `log.error`;
  - `lead: any`, `data: any` → `Lead` і `SheetsResponse`; сам об'єкт тепер
    типізований як `Integration`.
- Що змінилось у поведінці (має бути: нічого з того, що фіксують тести):
  URL лишився `SHEETS_WEBHOOK_URL + "?token=" + SHEETS_TOKEN`, тіло запиту те саме
  (`values: [[createdAt, name, email, phone ?? "", source]]`), текст помилки той
  самий (`"sheets error: " + status`), експорт лишився **default**. Тести не
  редаговані: `git diff` по `*.test.ts` порожній.
  Поза тим, що фіксують тести, стало **краще**: мережевий збій раніше кидав
  виняток назовні (порушення «send завжди повертає `Result`»), тепер повертається
  `Result{ok:false}`; рядок журналу приведено до формату сусіднього
  `slack-notify.ts`, і токен у URL більше не потрапляє в журнал — `log.redact()`
  маскує `?token=`.

#### Прогін 3.2 — `/refactor app/src/sync/state.ts` (**справжній виклик через `/`**)

- Виклик: `/refactor app/src/sync/state.ts`, 20.09.2026.
- **Чи підставився `$ARGUMENTS`: так** — тіло команди прийшло з розкритим
  рядком `**Ціль:** app/src/sync/state.ts`.
- `check:rules` для цього файлу: **1 → 0** (`json-via-parse` line 14).
  `TOTAL` по проєкту: **1 → 0**.
- `npm test`: **30 passed (9 файлів)** до і після, кількість та сама.
  `typecheck`: чисто.
- Замінено: `JSON.parse(readFileSync(...))` → `parseJson(text, isSyncState)` з
  guard на `{ lastSyncedAt: string }` (`isRecord` + `isString`); тихий `catch` →
  `log.error` із текстом, що курсор скинуто і наступний прогін розішле все заново.
- **Найтонше місце щодо «без зміни поведінки»:** в оригіналі `try/catch` накривав
  **і** `readFileSync`, тож збій читання (права, зникнення файлу між `existsSync`
  і читанням) теж давав `INITIAL_STATE`. Якби читання вийшло з-під `try`, воно
  почало б кидати назовні — зміна поведінки, якої **жоден тест не ловить**
  (`run.test.ts` перевіряє лише щасливі шляхи). Тому читання лишилось під власним
  `catch`.
- **Stop-правило спрацювало частково — і це головний результат прогону.**
  Повністю привести файл до конвенцій не вдалося: пункт «не підставляй дефолт
  мовчки» вимагає віддавати помилку як `Result{ok:false}`, а це змінює сигнатуру
  `loadState` на `Result<SyncState>` і поведінку `runSync`. Команда це прямо
  забороняє, тож рефакторинг зробив усе можливе без зміни поведінки, а решту
  залишив із попереджувальним коментарем на початку `state.ts`.
- ⚠️ **`TOTAL: 0` не означає, що інцидент виправлено.** Тихий відкат курсора на
  місці — просто тепер він гучний у журналі. Раніше маркером дефекту служило
  видиме порушення `check:rules`; тепер маркер — коментар у `state.ts` і цей
  запис. Це й додано в `AGENTS.md` і `conventions.md` окремим попередженням:
  чистий звіт перевірки ≠ код відповідає конвенціям.
- Acceptance criteria: порушень у файлі стало менше (1 → 0), у решті не більше;
  тести зелені до і після з тією самою кількістю; typecheck чистий; асерти в
  тестах не редаговані (`git diff -- '*.test.ts'` порожній); змінено рівно один
  файл; `core` не зачеплено.

### `/generate-integration` — два прогони

Команду запускали двічі, на сервісах **різного типу**. Це не дублювання: крок 4
команди («вирішу, що це за система») веде до протилежних рішень щодо даних ліда,
і два прогони показують, що правило застосовується вибірково, а не механічно.

#### Прогін 2.1 — `/generate-integration telegram-notify` (за текстом команди)
- Які файли створено:
  - `app/src/integrations/telegram-notify.ts` — `Integration` з
    `name: "telegram-notify"`, `requiredEnv: ["TELEGRAM_BOT_TOKEN", "TELEGRAM_CHAT_ID"]`;
  - `app/src/integrations/telegram-notify.test.ts` — 4 тести;
  - один рядок у `app/src/integrations/index.ts` (імпорт + елемент масиву).
- **Мінімізація даних:** Telegram — месенджер, не система обліку, тому в тіло
  запиту йдуть лише `name`, `source`, `budgetUsd`. `email` і `phone` не
  передаються — це окремо зафіксовано тестом
  («форматує повідомлення без email і телефону»).
- `npm test`: **22 passed (7 файлів)** — 18 старих + 4 нових, усі зелені.
- `npm run check:rules`: `TOTAL: 1` — **нових порушень немає** (лишилось те саме
  `state.ts:14`, яке команда `analyze-error` свідомо не чіпала).
- `npm run typecheck`: без помилок. `package.json` не змінено, `npm install` не запускався.

#### Прогін 2.2 — `/generate-integration hubspot-sync` (**справжній виклик через `/`**)

- Виклик: `/generate-integration hubspot-sync`, 20.09.2026, у сесії, де команда
  вже зареєстрована.
- **Чи підставився `$ARGUMENTS`: так** — тіло команди прийшло з розкритим
  рядком `**Ціль:** hubspot-sync`.
- Які файли створено:
  - `app/src/integrations/hubspot-sync.ts` — `Integration` з
    `name: "hubspot-sync"`, `requiredEnv: ["HUBSPOT_TOKEN"]`;
  - `app/src/integrations/hubspot-sync.test.ts` — 4 тести;
  - один рядок у `app/src/integrations/index.ts`.
- **Мінімізація даних — протилежне рішення до прогону 2.1:** HubSpot це CRM,
  тобто **система обліку**, тому в запит іде повний лід — `email`, `firstname`,
  `phone`, `lead_source`, `budget_usd`. Конвенція відрізає email і телефон лише
  для сповіщень; для систем обліку вона їх дозволяє. Порівняння двох прогонів і
  є доказом, що команда змушує зробити цей вибір явно, а не скопіювати сусідній
  модуль.
- **Робота з секретом:** токен іде заголовком `Authorization: Bearer …` через
  `PostOptions.headers`, а не в query. У `sheets-append` він у URL і його рятує
  `log.redact()`; тут маскувати нема чого — у журнал токен не потрапляє взагалі.
  Тест перевіряє сам заголовок.
- `npm test`: **22 → 26 passed** (7 → 8 файлів).
- `npm run typecheck`: без помилок.
- `npm run check:rules`: `TOTAL: 1` → **`TOTAL: 1`** — не виріс.
- Acceptance criteria — усі шість виконані, зокрема перевірені окремо:
  `git status --short` показав рівно три зміни; `grep` по новому файлу на
  `fetch` / `process.env` / `JSON.parse` / `console.*` / `any` — порожній;
  `git diff` по `package.json` і `package-lock.json` — без змін.
- **Stop:** `app/src/core/**` не чіпали — контракт `Integration` і тип `Lead`
  узято як є, усе потрібне в них знайшлось; `npm install` не запускався.

---

## Task E (бонус) — хук

- Файли: `.claude/settings.json` (`PreToolUse`, matcher `Edit|Write|NotebookEdit`)
  і `.claude/hooks/protect-core.mjs` (Node, щоб працювало на Windows).
- Як працює: читає JSON зі stdin, бере шлях із `tool_input.file_path` (із запасними
  `filePath` / `path` / `notebook_path`), нормалізує його відносно `cwd`, і якщо він
  у захищеній зоні — пише причину в **stderr** і виходить з кодом **2**.
- Захищені зони в хуку: `app/src/core`, `app/scripts`, `materials`, `.github`,
  `.coderabbit.yaml` — ті самі, що в `.claude/rules/do-not-touch.md`.
- Спроба змінити `app/src/core/log.ts` → відповідь хука (цитата, exit code 2):

  ```
  Заблоковано: app/src/core/log.ts — захищена зона проєкту (app/src/core).
  Правило .claude/rules/do-not-touch.md: ці файли не редагуються, винятків немає.
  Зупинись і опиши людині, яка саме зміна тут потрібна і навіщо, замість того щоб її робити.
  ```

- Перевірені гілки скрипта (вручну, через stdin): відносний шлях у захищеній зоні
  → `exit 2`; абсолютний шлях (`.../app/scripts/check-rules.mjs`) → `exit 2`;
  `materials/ab-task.md` → `exit 2`; дозволений шлях
  (`app/src/integrations/sheets-append.ts`) → `exit 0`; порожній stdin → `exit 0`
  (хук нічого не ламає, якщо форма вводу незнайома).

### Жива перевірка — хук зупинив справжню спробу

Перевірка через stdin доводить лише логіку скрипта. Нижче — спроба реального
редагування інструментом `Edit`, у сесії з активним хуком (20.09.2026, після
повернення `settings.json` з `.off`).

- **Спроба:** додати коментар на початок `app/src/core/log.ts` — рівно той сценарій,
  що описаний в умові Task E.
- **Результат: дію заблоковано.** Інструмент не виконався, повернулась помилка хука:

  ```
  PreToolUse:Edit hook error: [node .claude/hooks/protect-core.mjs]:
  Заблоковано: app/src/core/log.ts — захищена зона проєкту (app/src/core).
  Правило .claude/rules/do-not-touch.md: ці файли не редагуються, винятків немає.
  Зупинись і опиши людині, яка саме зміна тут потрібна і навіщо, замість того щоб її робити.
  ```

- **Доказ, що файл не постраждав:** `git hash-object app/src/core/log.ts` до і після
  спроби — той самий `a0c49f7b077878fdf75cf7d6df88f1c951d7ed36`;
  `git status --short` порожній; `check:rules` → `core-untouched 0`.
- **Негативний контроль (немає хибних спрацювань):** цей самий запис у
  `docs/verification.md` зроблено тим самим інструментом `Edit` у тій самій сесії —
  і він пройшов. Тобто хук блокує саме захищені зони, а не редагування взагалі.

**Правило проти хука — у чому різниця.** `do-not-touch.md` **просить** не чіпати
ядро, і прогін B у Task D показав, що прохання можна обійти з добрими намірами:
агент без правил змінив `types.ts` і чесно про це повідомив. Хук не просить — він
не дає: інструмент не виконується взагалі, і агент отримує текст правила як
зворотний зв'язок замість результату. Це та сама теза WS2 «права надійніші за
правила», але вже з доказом на власному коді.

---

## Рев'ю CodeRabbit — розбір семи зауважень

Рев'ю дало 7 actionable-коментарів. Три виправлені, одне виправлене інакше, ніж
пропонувалось, два не виправляються кодом, одне відкрите.

| # | Зауваження | Рішення |
|---|---|---|
| 1 | `architecture.md`: `grep` на імпорти з `../sync/` ловив лише подвійні лапки | ✅ виправлено на `grep -rnE "from ['\"]\.\./sync/"` |
| 2 | `console.*` у тестах порушує правило | ✅ виправлено **правило**, не тести — див. нижче |
| 3 | `JSON.parse` у тестах порушує правило | ✅ те саме уточнення області дії |
| 4 | Неідемпотентні POST повторюються (дублікати) | ⚠️ код не змінено, обґрунтування нижче |
| 5 | Секрети в URL журналюються (CWE-532) | ❌ факту не відповідає; додано тест-доказ |
| 6 | Прогін A не в чистій сесії | ✅ перероблено: агент зі свіжим контекстом, лише дослівний запит |
| 7 | Немає справжнього прогону `/refactor` | ✅ запущено через `/`, прогін 3.2 вище |

### #2 і #3 — чому виправлено правило, а не тести

Зауваження знайшло справжню суперечність: `conventions.md` забороняв `console.*`
і `JSON.parse` для всього `app/src/**/*.ts`, а тести **стартового** репо
(`slack-notify.test.ts`, `sheets-append.test.ts`) роблять і те, і те. Тобто моє
правило суперечило коду, який уже лежав у проєкті, — це дефект правила.

Виправлено область дії: заборони стосуються робочого коду, а в тестах дозволені
рівно два випадки — приглушити журнал через `vi.spyOn(console, "log")` і
розібрати **перехоплене власне** тіло запиту через `JSON.parse` + `toEqual`.

Запропоновану заміну на `expect(init?.body).toBe(JSON.stringify(...))` **не
зробив**: порівняння серіалізованих рядків залежить від порядку ключів, тож тест
почав би падати від переставлення полів, яке нічого не змінює. Порівнювати треба
структуру, а не її текстове подання.

### #4 — чому не додано `retries: 0`

Зауваження по суті правильне: `postJson` за замовчуванням робить до трьох спроб, і
для неідемпотентних операцій (створити контакт, надіслати повідомлення, додати
рядок) втрачена відповідь після застосованої операції дасть дублікат. Іронічно,
нічний інцидент був саме про дублікати.

Код усе ж не змінено, з трьох причин:

1. Повтори на 5xx/429 — **документована політика платформи** з архітектурної
   записки, а не недогляд. Скасовувати її для окремих інтеграцій означає обміняти
   ризик дублікатів на ризик втрати доставки, і це рішення платформної команди.
2. `sheets-append.ts` під `retries: 0` змінив би поведінку, а Stop-правило
   `/refactor` це забороняє.
3. `slack-notify.ts` (стартовий код) лишається на дефолтах, тож зміна тільки
   нових інтеграцій зробила б проєкт неоднорідним.

Правильний хід за правилами самого проєкту — описати й віддати людині, а не
вирішити самому. Рекомендація для платформної команди: додати в `postJson`
підтримку ключа ідемпотентності або окремий `postJsonOnce`, і вже тоді перевести
всі створювальні інтеграції на нього.

### #5 — твердження перевірене й не підтвердилось

Зауваження каже, що `postJson` пише повний URL у журнал, тож токен у URL
протікає. Перевірено проти `core/log.ts`: `redact()` маскує **всі** форми
секретів, які використовують наші інтеграції:

```
POST https://api.telegram.org/bot123456789:AAHfake…/sendMessage  →  bot<REDACTED>
POST https://sheets.example.test/append?token=fake-sheets-token  →  ?token=<REDACTED>
Authorization: Bearer fake-hubspot-token-000000                  →  Bearer <REDACTED>
POST https://hooks.slack.example.test/services/T000/B000/fake    →  /services/<REDACTED>
```

Саме тому архітектурна записка й вимагає писати журнал лише через `log`. Щоб це
не трималось на слові, додано `app/src/integrations/secret-redaction.test.ts`.

**Але одна частина зауваження вказала на справжній дефект:** фікстура
`telegram-notify.test.ts` задавала `TELEGRAM_BOT_TOKEN` як `bot000000:…`, через що
URL виходив `…/botbot000000:…`. Реальні токени Bot API префікса `bot` не містять —
він належить шляху URL. Фікстуру виправлено на `000000:…`.

---

## Другий раунд рев'ю — ще чотири зауваження

Після пушу виправлень CodeRabbit перезапустили вручну (`@coderabbitai review` —
автоматичних рев'ю репо не отримує, бо має менше 10 зірок). Новий прохід дав
**4 зауваження, усі чотири по суті**. Усі виправлені.

| # | Зауваження | Що зроблено |
|---|---|---|
| 8 | Stop-правило `/refactor` згадує лише `app/src/core/**`, а захищених зон п'ять | ✅ розширено на всі п'ять + заборону чіпати `core.lock.json` |
| 9 | `conventions.md` бреше: «кожен пункт перевіряється `check:rules`» | ✅ розділено на шість автоматичних і перелік ручних |
| 10 | Guard `isSyncState` приймає порожній курсор | ✅ звужено до ISO-8601 UTC |
| 11 | Твердження, що тест маскування ловить зміну форми URL, хибне | ✅ додано наскрізні тести, які справді ловлять |

### #10 — найсерйозніше з цього раунду

Зауваження знайшло **новий** дефект, який я сам і вніс попереднім рефакторингом.
`isSyncState` перевіряв лише `isString(value.lastSyncedAt)`, а порожній рядок цю
перевірку проходить. Далі в `runSync` іде `lead.createdAt > state.lastSyncedAt`, і
проти `""` це істинне **для будь-якого** ліда. Тобто структурно «справний» файл
стану з `{"lastSyncedAt": ""}` давав той самий шторм дублікатів, що й побитий, —
але вже без жодного запису в журнал, бо guard його пропускав.

Guard звужено до форми, яку реально пише `saveState()`:

```
""                          → відхиляється, log.error
"2026"                      → відхиляється, log.error
"garbage"                   → відхиляється, log.error
"1970-01-01T00:00:00.000Z"  → приймається
"2026-09-10T08:00:00.000Z"  → приймається
```

Це не усуває задокументоване обмеження (повернення `INITIAL_STATE` замість
`Result{ok:false}` — потребує зміни поведінки), але прибирає мовчазний шлях:
тепер будь-який непридатний курсор помітний у журналі.

### #12 — третій раунд: guard перевіряв лише форму, не календар

Повторне рев'ю підтвердило три виправлення з раунду 2 і лишило одне: regex
`ISO_UTC` перевіряв **текстову форму**, а не те, чи дата існує. Перевірив
емпірично — картина навіть гірша, ніж у зауваженні:

| Значення | regex | `Date.parse` |
|---|---|---|
| `0000-00-00T00:00:00Z` | проходить | `NaN` |
| `2026-99-99T99:99:99Z` | проходить | `NaN` |
| `2026-13-01T00:00:00Z` | проходить | `NaN` |
| **`2026-02-30T00:00:00Z`** | **проходить** | **`2026-03-02` — мовчки зсунуто** |

Тобто перевірки на `NaN` замало: `2026-02-30` розбирається успішно і «перевертає»
курсор на два дні вперед, через що частина лідів тихо **не** розсилається. Guard
тепер тришаровий: форма → `Date.parse` → **звіряння компонентів** із тим, що
реально розібрав `Date`.

Додано `app/src/sync/state.test.ts` — 12 тестів: три придатні курсори (з
мілісекундами, без них, відсутній файл) і вісім непридатних, кожен з перевіркою,
що курсор відкинуто **і** про це є запис у журналі.

**Мутаційна перевірка тесту.** Щоб не повторити помилку #11 і не заявити більше,
ніж тест робить, guard тимчасово послаблено назад до `isString` — впали рівно ті
5 тестів, що описують небезпечні курсори (порожній, `0000-00-00`, `2026-02-30`,
`2026-13-01`, «вчора»), решта 39 лишились зеленими. Після відновлення guard —
44/44.

### #11 — я справді переоцінив власний тест

У звіті було написано, що `secret-redaction.test.ts` впаде, якщо змінити форму
URL в інтеграції. Це неправда: тест передавав фіксовані рядки прямо в `redact()`,
жодної інтеграції не викликав і журналу `postJson` не бачив. Твердження
виправлено, і додано те, чого бракувало, — **наскрізні** тести: реальний виклик
`telegramNotify.send()` і `sheetsAppend.send()` з відповіддю `HTTP 500`
(саме 5xx, бо на 4xx `postJson` виходить із циклу ще до `log.warn`), перехоплення
`console` і перевірка, що в журнал пішло `bot<REDACTED>` / `?token=<REDACTED>`, а
самого токена там немає. Ось вони зміну форми URL уже ловлять.

---

## Статус перевірок

Claude Code реєструє `.claude/rules/`, `.claude/commands/` і `.claude/settings.json`
на **старті** сесії, тож перевірки нижче робились уже після перезапуску.

| # | Перевірка | Статус |
|---|---|---|
| 1 | Які файли інструкцій завантажено — чи працює `@AGENTS.md` | ✅ три файли, `AGENTS.md` підтягнувся через імпорт |
| 2 | Питання агентові про правила в контексті, без відкриття файлів | ✅ назвав `do-not-touch` + база; `architecture`/`conventions` за `paths` не завантажені |
| 3 | `/analyze-error materials/error-log.txt` через `/` — чи підставився `$ARGUMENTS` | ✅ підставився; прогін виконано через `/`, Stop спрацював |
| 4 | Чи підтягуються `architecture`/`conventions` за `paths` під час роботи з кодом | ✅ зайшли самі, щойно прочитано `app/src/sync/state.ts` |
| 5 | `/generate-integration hubspot-sync` через `/` — чи підставився `$ARGUMENTS` | ✅ підставився; прогін виконано через `/`, усі 6 AC зелені |
| 6 | «Додай коментар на початок `app/src/core/log.ts`» — чи блокує хук живцем | ✅ `Edit` заблоковано хуком, файл не змінився (той самий hash) |

**Що з `/refactor`.** Запущено через `/` двічі — див. прогони 3.1 і 3.2 нижче.
